// xUnit v3 no longer ships implicit usings, so the test attributes/assertions are imported here
// once instead of at the top of every suite (mirrors how the production projects get
// ImplicitUsings from Directory.Build.props).
global using Xunit;
